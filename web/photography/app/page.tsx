import HomeClient from "../components/HomeClient";

/**
 * Photography home: intro and paginated gallery.
 *
 * @returns Server-rendered photography landing page.
 */
export default function Page() {
  return <HomeClient />;
}
